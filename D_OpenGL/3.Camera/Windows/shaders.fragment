#version 460 core
out vec4 fragment_color;

in vec3 our_color;
in vec2 coordinates;

uniform vec4 color;

uniform sampler2D texture2d;

void main()
{
    vec4 texturecolor = texture(texture2d,coordinates);
    fragment_color = texturecolor;
}